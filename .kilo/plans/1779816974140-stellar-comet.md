# Fix: Remove nested container-fluid from user pages

## Problem
Admin user pages that extend `layouts.admin.app` wrap their content in an extra `<div class="container-fluid">`, but the layout already provides one at line 88. This double-nesting breaks the full-width container layout.

## Root Cause
- `layouts/admin/app.blade.php` line 88: `<div class="container-fluid">@yield('content')</div>`
- `admin/users/edit.blade.php` line 5: adds another `<div class="container-fluid">` — **STILL NEEDS FIX**

Already fixed (previous session):
- ~~`admin/users/index.blade.php`~~
- ~~`admin/users/pending.blade.php`~~
- ~~`admin/users/show.blade.php`~~

## Fix
Remove the wrapping `<div class="container-fluid">` and its closing `</div>` from:

1. **`resources/views/admin/users/edit.blade.php`** — Remove `<div class="container-fluid">` (line 5) and closing `</div>` (line 205)

## Status: ✅ COMPLETED
All user pages now have consistent structure without double-nested container-fluid wrappers.
